apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - namespace.yaml

patchesStrategicMerge:
  - frontend-patch.yaml
  - backend-patch.yaml
  - postgres-patch.yaml

images:
  - name: ghcr.io/nadzzdan/frontend
    newTag: latest
  - name: ghcr.io/nadzzdan/backend
    newTag: latest
  - name: ghcr.io/nadzzdan/database
    newTag: latest

namePrefix: prod-
namespace: production

commonLabels:
  environment: production